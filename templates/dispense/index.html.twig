{% extends 'base.html.twig' %}

{% block title %}Dispense index{% endblock %}

{% block body %}
    <h1>Dispense index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>StartTime</th>
                <th>EndTime</th>
                <th>Day</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for dispense in dispenses %}
            <tr>
                <td>{{ dispense.id }}</td>
                <td>{{ dispense.startTime }}</td>
                <td>{{ dispense.EndTime }}</td>
                <td>{{ dispense.day }}</td>
                <td>
                    <a href="{{ path('app_dispense_show', {'id': dispense.id}) }}">show</a>
                    <a href="{{ path('app_dispense_edit', {'id': dispense.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_dispense_new') }}">Create new</a>
{% endblock %}
